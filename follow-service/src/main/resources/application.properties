spring.application.name=follow-service
server.port=8085

spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:3306/connectsphere_follow_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

eureka.client.service-url.defaultZone=${EUREKA_URL}

spring.rabbitmq.host=${RABBITMQ_HOST}
spring.rabbitmq.port=${RABBITMQ_PORT}
spring.rabbitmq.username=${RABBITMQ_USERNAME}
spring.rabbitmq.password=${RABBITMQ_PASSWORD}
connectsphere.rabbitmq.notification-exchange=connectsphere.notifications.exchange
connectsphere.rabbitmq.new-follower-routing-key=notification.follow.created

management.endpoints.web.exposure.include=*
logging.file.name=logs/follow-service.log
